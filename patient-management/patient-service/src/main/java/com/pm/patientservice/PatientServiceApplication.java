package com.pm.patientservice;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class PatientServiceApplication {

    public static void main(String[] args) {
        SpringApplication.run(PatientServiceApplication.class, args);
    }

}
// This is the main application class for the Patient Service microservice.
// It uses Spring Boot's @SpringBootApplication annotation to enable auto-configuration
// and component scanning. The main method starts the application.
// ****This is the main entry point for the Patient Service microservice.

